{
  "name": "subgraph-genadrop",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ doochain/subgraph-genadrop",
    "create-local": "graph create --node http://localhost:8020/ doochain/subgraph-genadrop",
    "remove-local": "graph remove --node http://localhost:8020/ doochain/subgraph-genadrop",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 doochain/subgraph-genadrop",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.53.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^20.4.5",
    "matchstick-as": "0.5.0"
  }
}
